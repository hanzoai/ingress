name: Sync Docker Images

# Disabled: this workflow syncs upstream traefik images which is not needed.
# Keep workflow_dispatch for manual use if needed.
on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      packages: write
      contents: read
    if: github.repository == 'hanzoai/ingress'

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - uses: imjasonh/setup-crane@31b88efe9de28ae0ffa220711af4b60be9435f6e # v0.4

      - name: Sync
        run: |
          echo "No sync configured for hanzoai/ingress yet."
          echo "To sync images, update this workflow with the correct source/target registries."
